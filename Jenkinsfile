@Library('jenkinslib')

def tools = new org.devops.tools()
pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                script{
			echo "hostname -I"
				}
            }
        }
    }
}
